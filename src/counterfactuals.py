"""Session 7 — What-if tab."""

from __future__ import annotations

from sklearn.pipeline import Pipeline


def suggest_what_ifs(
    participant_id,
    today: dict,
    last_night: dict,
    baselines,
    pipe: Pipeline,
    current_proba: float,
):
    raise NotImplementedError("Session 7 — see solutions/counterfactuals.py")
